plugins {
    id 'java'
}

group 'rsql'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

apply plugin: 'idea'
dependencies {
    annotationProcessor group: 'org.antlr', name: 'antlr4', version: '4.11.1'
    implementation group: 'org.antlr', name: 'antlr4-runtime', version: '4.11.1'
    testImplementation group: 'junit', name: 'junit', version: '4.12'
}

idea {
    module {
        sourceDirs += file("src/main/gen")
    }
}
